typedef struct Matrix{
        int** num; 
        int row;
        int column;
        }Matrix;
Matrix* empty_matrix(int n,int m);
Matrix* somme(Matrix*,Matrix*);
Matrix* somme(Matrix*,Matrix*);
void print_matrix(Matrix*); 
